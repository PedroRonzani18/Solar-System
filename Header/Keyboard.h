#ifndef _KEYBOARD_H
#define _KEYBOARD_H

// Simula as teclas de um teclado
class Keyboard
{
    public:
        Keyboard();

        int w,a,s,d;
        int space,f,k;
        int z,x,c,v,b,n,l;
};

#endif